export default function StatusPill({ status }: { status: string }) { const key = status.toLowerCase(); const color = key.includes('overdue') || key.includes('absent') || key.includes('danger') ? 'bg-red-50 text-status-danger' : key.includes('pending') || key.includes('warning') ? 'bg-amber-50 text-status-warning' : key.includes('paid') || key.includes('present') || key.includes('active') || key.includes('success') ? 'bg-green-50 text-status-success' : 'bg-accent-blue-soft text-status-info'; return <span className={`inline-flex rounded-pill px-2.5 py-1 text-xs font-semibold capitalize ${color}`}>{status.replaceAll('_', ' ')}</span> }
