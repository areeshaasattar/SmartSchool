import type { ReactNode } from 'react'
export default function StatCard({ label, value, icon, tone = 'blue', trend }: { label: string; value: ReactNode; icon?: ReactNode; tone?: 'blue' | 'purple' | 'yellow'; trend?: string }) { const colors = { blue: 'bg-accent-blue-soft', purple: 'bg-accent-purple-soft', yellow: 'bg-accent-yellow-soft' }; return <div className={`rounded-card p-6 ${colors[tone]}`}><div className="flex items-start justify-between"><div><p className="text-sm font-medium text-text-body">{label}</p><p className="mt-2 text-3xl font-bold text-text-heading">{value}</p>{trend && <span className="mt-3 inline-block text-xs font-medium text-status-success">↑ {trend}</span>}</div>{icon && <div className="text-accent-blue">{icon}</div>}</div></div> }
