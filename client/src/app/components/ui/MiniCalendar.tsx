export default function MiniCalendar({ events = [] }: { events?: Array<{ date: string; title: string; type?: 'info' | 'warning' | 'success' }> }) { const today = new Date(); const days = Array.from({ length: 7 }, (_, index) => new Date(today.getFullYear(), today.getMonth(), today.getDate() - today.getDay() + index)); const borders = { info: 'border-accent-blue', warning: 'border-status-warning', success: 'border-status-success' }; return <div className="rounded-card bg-surface p-5 shadow-sm"><h2 className="font-semibold text-text-heading">This week</h2><div className="mt-4 grid grid-cols-7 gap-1 text-center text-xs">{days.map((day) => <div key={day.toISOString()} className={`rounded-full py-2 ${day.toDateString() === today.toDateString() ? 'bg-accent-blue text-white' : 'text-text-body'}`}>{day.getDate()}</div>)}</div><div className="mt-5 space-y-2">{events.length ? events.map((event) => <div key={`${event.date}-${event.title}`} className={`border-l-2 ${borders[event.type ?? 'info']} pl-3 text-sm`}><p className="font-medium text-text-heading">{event.title}</p><p className="text-xs text-text-muted">{event.date}</p></div>) : <p className="text-sm text-text-muted">No upcoming events</p>}</div></div> }
