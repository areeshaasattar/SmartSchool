export default function Avatar({ name, src, online = false }: { name: string; src?: string; online?: boolean }) { const initials = name.split(' ').map((part) => part[0]).slice(0, 2).join('').toUpperCase(); return <div className="relative grid h-10 w-10 place-items-center overflow-hidden rounded-full bg-accent-purple-soft text-sm font-semibold text-accent-purple">{src ? <img src={src} alt={name} className="h-full w-full object-cover" /> : initials}{online && <span className="absolute bottom-0 right-0 h-3 w-3 rounded-full border-2 border-white bg-status-success" />}</div> }
